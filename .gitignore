node_modules
.DS_Store
server/public
vite.config.ts.*

# Local secrets — NEVER commit (Render API key, generated passwords, etc.)
.secrets/
*.tar.gz
*.db
*.db-shm
*.db-wal
.env
.env.local

# Internal dev notes
EXPLORATION.md
FINDINGS.md
profile.json

# Claude Code config
.claude/
